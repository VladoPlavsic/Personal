For server side we used uvicorn.

To run server:<br>
1. get docker<br>
2. navigate to root folder (the one containing docker-compose.yaml)<br>
3. run following command: <h4><code>docker-compose up --build</code></h4>

